import styled from 'styled-components';

import StyledFormHelperText from './index';

const InlineFormHelperText = styled(StyledFormHelperText)`
  display: inline;
`;

export default InlineFormHelperText;
